/**
 * An object defining all page routes.page.
 */

export const page = {
	index: "/"
};

export const build = {
	// user(username) {
	// 	return `/${username}`;
	// },
};

export const api = {
	user: "/api/user"
};
