import { Command } from "@oclif/core";

export default class Arcampaigns extends Command {
	static description = "CLI to control the Network ArCampaigns Stream";

	static examples = ["<%= config.bin %> <%= command.id %>"];

	public async run() {
		return null;
	}
}
